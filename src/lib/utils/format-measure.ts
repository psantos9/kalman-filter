/**
 * @typedef {number | Array.<number>} Observation
 */
/**
 * If m is a number, result will be [m]
 * If m is an Array.<Number>, result will m
 * @param {Observation} m
 * @returns {Array.<number>}
 */
module.exports = function (m) {}
